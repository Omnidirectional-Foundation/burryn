// lex_match.bur — lex_match。
// lex_match.bur — lex_match.


// lex_match 若下一字符匹配则消费并返回 true / lex_match consumes c if it matches next
fn lex_match(src, mut st, c) {
    if lex_peek(src, st) == c {
        st[1] = st[1] + 1
        true
    } else {
        false
    }
}
