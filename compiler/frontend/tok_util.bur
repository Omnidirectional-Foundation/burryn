// tok_util.bur — token 访问器。
// tok_util.bur — token accessors.

import "burryn.dev/compiler/lib"

// tok_type 返回 token 的类型编号 / tok_type returns the token type id
pub fn tok_type(t) {
    match t {
        lib::Token::Tok(ty, _, _, _) => ty,
    }
}
