// cgen_num.bur — C 数字/字符串字面量。
// cgen_num.bur — C number/string literals.

// hex_digit 把 0-15 转 16 进制字符 / hex_digit renders 0-15 as a hex character
fn hex_digit(n) {
    if n < 10 {
        chr(48 + n)
    } else {
        chr(65 + n - 10)
    }
}
