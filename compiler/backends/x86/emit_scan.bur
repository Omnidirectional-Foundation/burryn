// emit_scan.bur — x86 指令扫描辅助（长度、跳转目标、局部计数）。
// emit_scan.bur — x86 instruction scan helpers (length, jump targets, locals).

import "burryn.dev/compiler/lib"

// read_u16 读 u16 操作数 / read_u16 reads a u16 operand
// Read a u16 operand
fn read_u16(code, at) {
    code[at] * 256 + code[at + 1]
}


