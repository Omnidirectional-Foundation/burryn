// runtime.bur — x86 运行时机器码块：GC、调度、channel、waitset、timer、_start。
// runtime.bur — x86 runtime machine-code blocks: GC, sched, channel, waitset, timer, _start.

// gc_rec 生成分配记录插桩（rdi=ptr 寄存器, size 立即数或寄存器；保存调用方 rdi/rsi）
// gc_rec emits the alloc-recording stub (rdi=ptr register, size immediate
// or register; preserves caller rdi/rsi)

// GC / fiber / sched 已拆至 runtime_gc.bur · runtime_fiber.bur · runtime_sched.bur。
// GC / fiber / sched live in runtime_gc.bur · runtime_fiber.bur · runtime_sched.bur.
