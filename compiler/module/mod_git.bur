// mod_git.bur — git/exec/parent。
// mod_git.bur — git/exec/parent.


// mod_git_base 取 git 基址（BURGITBASE 或 ~/.burryn）
// mod_git_base returns the git base (BURGITBASE or ~/.burryn)
fn mod_git_base() {
    match env_lookup("BURGITBASE") {
        Some(b) => b,
        None => "https://",
    }
}


