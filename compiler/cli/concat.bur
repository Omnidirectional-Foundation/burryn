// cli/concat.bur — 列表拼接。
// cli/concat.bur — list concat.

// concat_lists 拼接列表 / concat_lists concatenates lists
fn concat_lists(xss) {
    let mut acc = []
    for xs in xss {
        for x in xs {
            push(acc, x)
        }
    }
    acc
}
