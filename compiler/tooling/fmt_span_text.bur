// fmt_span_text.bur — span_text。
// fmt_span_text.bur — span_text.

import "burryn.dev/compiler/frontend"

// span_text 取区间文本 / span_text returns the span text
fn span_text(src, sp) {
    match sp {
        frontend::SpanT::Sp(s, e) => substr(src, s, e - s),
    }
}
