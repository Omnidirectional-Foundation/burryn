// fmt_indent.bur — 缩进。
// fmt_indent.bur — indent.

// push_indent 推入缩进 / push_indent pushes indentation
fn push_indent(mut buf, mut n) {
    while n > 0 {
        push(buf, " ")
        n = n - 1
    }
}
