// dump_sp.bur — span dump。
// dump_sp.bur — span dump.

import "burryn.dev/compiler/frontend"

// dump_sp 打印区间 / dump_sp prints a span
fn dump_sp(sp) {
    match sp {
        frontend::SpanT::Sp(s, e) => str(s) + ":" + str(e),
    }
}
