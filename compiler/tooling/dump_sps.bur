// dump_sps.bur — dump_sps。
// dump_sps.bur — dump_sps.

// dump_sps 打印区间列表 / dump_sps prints a span list
fn dump_sps(xs) {
    let mut parts = []
    for x in xs {
        push(parts, dump_sp(x))
    }
    "[" + join(parts, " ") + "]"
}
