// lsp_jstr.bur — jstr/jint。
// lsp_jstr.bur — jstr/jint.

import json "std/json"

// jstr 解包 JSON 字符串 / jstr unwraps a JSON string
fn jstr(v) {
    match v {
        json::Json::JStr(s) => s,
        _ => "",
    }
}

// jint 解包 JSON int / jint unwraps a JSON int
fn jint(v) {
    match v {
        json::Json::JInt(n) => n,
        _ => 0,
    }
}
