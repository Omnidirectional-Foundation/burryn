// fmt_span.bur — token/span 工具。
// fmt_span.bur — token/span helpers.

import "burryn.dev/compiler/lib"

// tok_start 取 token 起点 / tok_start returns the token start
fn tok_start(t) {
    match t {
        lib::Token::Tok(_, _, s, _) => s,
    }
}
