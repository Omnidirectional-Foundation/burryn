// lsp_json.bur — LSP JSON 取值辅助。
// lsp_json.bur — LSP JSON field helpers.

import json "std/json"

// jobj_get 按键取 JSON 对象值 / jobj_get reads a JSON object value by key
fn jobj_get(v, key) {
    match v {
        json::Json::JObj(keys, vals) => json::get(keys, vals, key),
        _ => None,
    }
}


