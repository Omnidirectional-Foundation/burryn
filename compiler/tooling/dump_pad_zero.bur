// dump_pad_zero.bur — pad_zero。
// dump_pad_zero.bur — pad_zero.

// pad_zero 零填充 / pad_zero zero-pads
fn pad_zero(n, w) {
    let mut out = str(n)
    while str_len(out) < w {
        out = "0" + out
    }
    out
}
