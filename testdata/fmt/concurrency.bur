enum Status{Active,Idle,Done}
fn worker(ch,n){
for i in range(0,n){
ch<-i
}
close(ch)
}
fn main_loop(){
let ch=chan(4)
spawn worker(ch,3)
select{
v=<-ch=>{println("got:",v)},
}
let mut sum=0
for v in ch{sum=sum+v}
println("rest sum:",sum)
}
main_loop()
