pub fn greet(name) {
    "hi, " + name
}

pub enum Greeting { Greeting(str) }

impl Greeting {
    fn (g: Greeting) shout() {
        match g {
            Greeting(msg) => msg + "!",
        }
    }
}
