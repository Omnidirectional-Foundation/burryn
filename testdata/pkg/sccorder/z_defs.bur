fn z_last(xs) {
    if len(xs) == 1 {
        xs[0]
    } else {
        z_last(slice(xs, 1, len(xs)))
    }
}

fn ping(xs) {
    if len(xs) == 0 {
        xs
    } else {
        pong(slice(xs, 1, len(xs)))
    }
}

fn pong(xs) {
    ping(xs)
}
