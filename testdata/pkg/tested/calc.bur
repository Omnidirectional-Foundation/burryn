// a main-less library package: `bur test` supplies its own entry point,
// and `*_test.bur` files never take part in a normal build

pub fn add(a, b) {
    a + b
}

pub fn scale(xs, k) {
    let mut out = []
    for x in xs {
        push(out, x * k)
    }
    out
}
