fn test_add() {
    assert(add(2, 3) == 5, "add(2, 3) should be 5")
}

fn test_scale() {
    let ys = scale([1, 2, 3], 10)
    assert(len(ys) == 3, "length preserved")
    assert(ys[2] == 30, "3 * 10")
}
