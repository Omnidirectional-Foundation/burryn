// enum fields may reference enums registered in a later file of the same
// package: registration collects every name first, validation runs second

enum Outer {
    Wrap(Inner),
}

fn main() {
    match Wrap(Iv(7)) {
        Wrap(i) => match i {
            Iv(n) => println(str(n)),
        },
    }
}
