fn main() {
    println(greeting(Happy, "burryn"))
    println(greeting(Grim(3), "gopher"))
    println("emphasis" + bangs(1))
    for m in [Happy, Grim(1), Grim(2)] {
        match m {
            Happy => println("mood: happy"),
            Grim(n) => println("mood: grim", n),
        }
    }
}
