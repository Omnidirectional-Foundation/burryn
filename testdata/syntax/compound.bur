// 复合赋值 + 标签循环 + for 索引
let mut sum = 0
outer: for i in range(0, 4) {
    for j in range(0, 4) {
        if i * j >= 6 { break outer }
        sum += 1
    }
}
let xs = ["a", "b"]
for idx, x in xs {
    println(idx, x)
}
println("sum", sum)
