// select_recv.bur — x86 select: recv 臂值类型（直线代码，println 内联 int→str 转换）
// select_recv.bur — x86 select: recv-arm value type (straight-line code, println inline int->str)
let mut ch = chan(2)
ch <- 42
select {
  x = <-ch => println("got", x)
}
exit(0)