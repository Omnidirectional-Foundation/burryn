// map：get/put/keys/delete
let mut m = map()
put(m, "a", 1)
put(m, "b", 2)
let got = match get(m, "a") {
    Some(v) => v,
    None => -1,
}
let ks = keys(m)
delete(m, "b")
let after = len(keys(m))
println(got, len(ks), after)
