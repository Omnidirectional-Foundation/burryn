// spin.bur — 常驻不退出，供 CI 读取进程实际映像基址（PIE 随机定基验收）。
// Never exits, so CI can read the process's real image base (PIE base check).

fn spin() {
    let mut i = 0
    while true {
        i = i + 1
    }
}

spin()
