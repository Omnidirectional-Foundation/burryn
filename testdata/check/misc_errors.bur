pub fn exported() { 1 }
pub let value = 2
pub enum E { A }
import "some/path"
enum Dup { X }
enum Dup { Y }
enum BadField { V(NoSuchType, [MissingToo], map(str), chan(int, int), int(str)) }
enum Generic(a) { Box(a, a(int)) }
fn nested() {
    enum Inner { Z }
    let r = 5?
    r
}
return 3
let t = chan(1, 2)
let u = 3?
let unused_one = 9
fn unused_fn() { 0 }
let opt = get(map(), "k")
fn drops_result() {
    read_file("x")
    0
}
let _keep = nested()
let _d = drops_result()
