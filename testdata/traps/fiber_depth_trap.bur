// fiber 内调用深度上限（入口帧不计） / call-depth cap inside a fiber (the entry frame does not count)
fn f(n) {
    if n == 0 {
        return 0
    }
    1 + f(n - 1)
}
fn g(c, n) {
    c <- f(n)
}
let c = chan(1)
spawn g(c, 2047)
println(<-c)
spawn g(c, 2048)
println(<-c)
