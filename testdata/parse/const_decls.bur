const answer = 42
const greeting = "hello" + ", " + "world"
pub const limit = answer * 2 + 1

fn demo(x) {
    const base = answer - 40
    const scaled = base * 3
    x + scaled
}

println(str(demo(1)))
