// chan_send_trap.bur — 向已关闭 channel 发送 → trap（exit 4，无 golden）/
// Sending on a closed channel traps (exit 4, no golden)
fn main_trap() {
    let ch = chan(1)
    close(ch)
    ch <- 1
}

main_trap()
