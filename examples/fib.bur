// recursion + a timed micro-benchmark
fn fib(n) {
    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }
}

let t0 = clock()
let r = fib(25)
let ms = (clock() - t0) * 1000.0

assert(r == 75025, "fib is broken")
println("fib(25) =", r)
println("took", ms, "ms on the BurrynVM")
