// select_recv.bur — 单臂 recv select：send 后 recv，验证臂变量类型与值传递
// select_recv.bur — single-arm recv select: send then recv, exercising arm-variable
// typing and value delivery
let ch = chan(2)
ch <- 42
select {
    x = <-ch => println("got", x),
}
exit(0)
