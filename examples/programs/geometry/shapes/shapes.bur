// one directory = one package; pub exports across packages
pub enum Shape {
    Circle(float),
    Rect(int, int),
    Point,
}

pub fn area(s) {
    match s {
        Circle(r) => 3.14159 * r * r,
        Rect(w, h) => to_float(w * h),
        Point => 0.0,
    }
}
