// geometry/shapes/shapes.bur — 形状枚举与面积（pub 导出）
// Shape enum and area, pub-exported
pub enum Shape {
    Circle(float),
    Rect(int, int),
    Point,
}

pub fn area(s) {
    match s {
        Circle(r) => 3.14159 * r * r,
        Rect(w, h) => to_float(w * h),
        Point => 0.0,
    }
}
