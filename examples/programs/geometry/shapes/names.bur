// geometry/shapes/names.bur — 形状名称辅助（pub 导出）
// Shape naming helpers, pub-exported
pub fn describe(s) {
    match s {
        Shape::Circle(r) => "circle r=" + str(r),
        Shape::Rect(w, h) => "rect " + str(w) + "x" + str(h),
        Point => "a point",
    }
}
