// same package as shapes.bur: shared top-level scope, no import needed
pub fn describe(s) {
    match s {
        Shape.Circle(r) => "circle r=" + str(r),
        Shape.Rect(w, h) => "rect " + str(w) + "x" + str(h),
        Point => "a point",
    }
}
