// fib.bur — 递归微基准 / Recursion micro-benchmark
fn fib(n) {
    if n < 2 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

let r = fib(25)

assert(r == 75025, "fib is broken")
println("fib(25) =", r)
